ريد في واجهة المستخدمين اريد عند تحديد
الصلاحيات على الواجات اريد احدد اكثر من خيار مثل يعرض واضافة وتعديل وليس اختيار خيار واحد فقط
كذالك اريد  يكون قسم صلاحيات كل واجهة يكون في نفس السطر الموجود فيه اضهار الصفحات وليس تحت 
اريد يكون شكل الصفحة كاملة مرتب ومنظم
///////////////////////////
اكمل لي هذا التعديل داخل التطبيق tageep 
سأضيف الآن إصلاحًا محافظًا: إذا كانت قائمة المستخدمين كلها بلا كلمة مرور، سيعيد النظام كلمة مرور admin الافتراضية لمستخدم admin فقط. هذا لا يغير كلمات المرور الموجودة إذا كان هناك مستخدم واحد على الأقل لديه كلمة مرور.
/////////////////////
 اريد تنشى لي تاب جديد يكون داخل تاب الاعدادات اسم التاب الدوام والفترات ابحيث انشى دوام وانشى فتراتللدوام مع تحديد بداية كل فتره ونهايتها 
 واريد امكانية التعديل والاضافة والحذف 
 واريد أريد إضافة مفهوم جديد يسمى:
بداية اليوم يتم تحتيدة حسب سياسة كل شركة
Operational Day

اليوم التشغيلي لا يعتمد على الساعة 12:00 صباحاً.

يتم تحديد وقت بداية اليوم من إعدادات الدوام.
من:
07/06/2026 06:00

إلى:
08/06/2026 05:59

كلها تعتبر:

يوم الاثنين
07/06/2026
/////////////////////
عندي تطبيق مستقل باسم tageep  وواجهة تسجيل دخول مستقلة 

والان عند تسجيل الدخول يظهر خطاء تسجيل الدخول 

المستخدم admin  وكلمة المرور admin

اريد تبحث عن السبب في التطبيق 
/////////////////
cd /home/newsmart/frappe-bench2 && bench --site site1.local execute "tageep.api._get_state" 2>&1 | head -50
///////////////////////
وُجدت المشكلة! في قاعدة البيانات، المستخدم admin له كلمة مرور فارغة "". الدالة _restore_admin_password_if_all_empty لا تُسترجع كلمة المرور لأن مستخدم "منير" له كلمة مرور "123456".

المشكلة في منطق استعادة كلمة المرور: الدالة تتحقق إذا كانت جميع كلمات المرور فارغة، لكن المستخدم "منير" له كلمة مرور، لذا لا يتم استعادة كلمة مرور admin.
//////////////////
اريد تعيد لي التطبيق tageep الى الوضع الذي كان علية بتاريخ امس الساعة11 ونصف مساءا تاريخ 8/6/2026
////////////////////////////////
 اريد تقارن ملف api   الموجود في تطبيق tageep
   قارن طريقة المصادقة وتسجيل الدخول بالملف هذا المرفق
    اجعله نفس الملف المرفق قسم المصادقة وتسجيل الدخول فقط 
    ///////////////////////
    اريد في التعقيب الرئيسي عند  انشاء اجازة للموظف مثلا اجازة سنوية الفترة الثانية يحسب 0.5 ويخصم على الموضف نصف يوم من رصيد الاجازات كذالك يخصم من الايام الفعلية مثلا تكون الايام الفعلية بدل 6 ايام ترجع 5.5
ويتم اضافة النصف اليوم في عمود ايام الغياب في جدول التعقيب الرئيسي 
حتى يتم الخصم من الاجر اليومي 
كذالك قيس على نفس الفكرة اذا كانت الاجازة يوم كامل 
/////////////////////////////
تم إجراء جميع التعديلات المطلوبة في ملف `tageep.js` لحساب نصف يوم بشكل صحيح عند اختيار إجازة سنوية في فترة محددة:

### التغييرات التي تم تنفيذها:

1. **`getAbsenceTotalsForEmployee()`** - تم تعديلها لتحتسب الإجازة السنوية كغياب أيضاً، بحيث تظهر في عمود "أيام الغياب" وفي عمود "إجازات سنوية" في نفس الوقت.

2. **`renderReportTable()`** - تم إصلاح معادلة حساب أيام الحضور الفعلية في التقرير لمنع الخصم المزدوج لأيام الإجازة السنوية.

3. **خلايا التواريخ الديناميكية في `renderMainTable()`** - تم تعديل عرض الخلايا لتظهر `0.5س` بدلاً من `0.5غ` عند اختيار إجازة سنوية في فترة محددة مع بقاء الفترة الأخرى حاضر.

### مثال عملي للنتيجة:
عند اختيار موظف → إجازة سنوية → الفترة الأولى (في تعقيب يومي):
- يتم خصم 0.5 من رصيد الإجازات السنوية
- يُسجل 0.5 يوم غياب في عمود "أيام الغياب"
- في التعقيب الرئيسي: الأيام المتوقعة 6، أيام الغياب 0.5، الأيام الفعلية 5.5
- يظهر في خانة التاريخ المحدد `0.5س` (للإشارة إلى إجازة سنوية نصف يوم)
/////////////////////////
تم إصلاح جميع المشاكل بنجاح في ملف `tageep.js`. إليك ملخص الإصلاحات:

## المشكلة:
عند تحضير موظف في التعقيب اليومي (إجازة سنوية + فترة محددة) ثم العودة إلى التعقيب الرئيسي، كانت البيانات تظهر قديمة لأن الجدول لم يكن يتم تحديثه تلقائياً.

## الإصلاحات:

1. **`switchTab()`** - تمت إضافة إعادة رسم تلقائي للجداول عند التبديل بين التبويبات (main, daily, employees, etc.)

2. **`switchMainPanel()`** - تمت إضافة إعادة رسم تلقائي عند التبديل بين الأقسام الفرعية داخل التبويب الرئيسي (التعقيب ← التقارير)

3. **`getAbsenceTotalsForEmployee()`** - الآن تحتسب الإجازة السنوية كغياب أيضاً في التقارير

4. **`renderReportTable()`** - تم إصلاح حساب الأيام الفعلية لمنع الخصم المزدوج

5. **خلايا التواريخ الديناميكية** - تظهر `0.5س` بدلاً من `0.5غ` عند إجازة سنوية نصف يوم

### الآن عند اختيار: موظف → إجازة سنوية → الفترة الأولى:
- ✅ يخصم 0.5 من رصيد الإجازات السنوية
- ✅ يسجل 0.5 في عمود أيام الغياب
- ✅ يحتسب الأيام الفعلية = 5.5 بدلاً من 6
- ✅ يظهر `0.5س` في خانة التاريخ
- ✅ يتم تحديث الجدول مباشرة عند العودة إلى التعقيب الرئيسي
////////////////
أفهم المشكلة الآن. عند الضغط على Ctrl+P مباشرة، اختفى رأس الصفحة بالكامل لأن #printHeader أصبح display:none في HTML و CSS أيضاً. الحل الصحيح هو إضافة صف الشعار مباشرة إلى thead الأصلي للجدول بشكل دائم، بحيث يظهر عند Ctrl+P ويتكرر تلقائياً في كل صفحة.
///////////////////
الان راس الصفحة اختفى لم يظهر عند الطباعة وريد ان يتكرر الراس في كل صفحة يتم طباعتها
///////////////////
أفهم المشكلة الآن. عند الضغط على Ctrl+P مباشرة، اختفى رأس الصفحة بالكامل لأن `#printHeader` أصبح `display:none` في HTML و CSS أيضاً. الحل الصحيح هو إضافة صف الشعار مباشرة إلى `thead` الأصلي للجدول بشكل دائم، بحيث يظهر عند Ctrl+P ويتكرر تلقائياً في كل صفحة.
///////////////////////
```javascript

    // Update Print Header Date Range
    document.getElementById('printDateRange').innerText = `الفترة من: ${fFrom} إلى: ${fTo}`;

    let filteredEmp = db.employees.filter(e => {
=======
    // Update Print Header Date Range
    document.getElementById('printDateRange').innerText = `الفترة من: ${fFrom} إلى: ${fTo}`;

    // === تعديل: إضافة صف الشعار مباشرة إلى thead الأصلي للجدول ===
    // الكود الأصلي: لم يكن هناك صف شعار داخل thead، وكان printHeader عنصراً منفصلاً خارج الجدول
    // مما يمنع ظهوره عند Ctrl+P وتكراره في الصفحات اللاحقة أثناء الطباعة.
    // التعديل: إضافة صف (print-header-row) إلى thead مخفياً (display:none) ويظهر فقط عند الطباعة.
    const mainTable = document.querySelector('#attendance-panel table');
    if (mainTable) {
        const thead = mainTable.querySelector('thead');
        if (thead) {
            // إزالة أي صف شعار موجود مسبقاً لتجنب التكرار
            const oldRow = thead.querySelector('tr.print-header-row');
            if (oldRow) oldRow.remove();

            const firstRow = thead.querySelector('tr');
            const colCount = firstRow ? firstRow.cells.length : 1;
            const companyName = db.settings.companyName || '';
            const dateRange = document.getElementById('printDateRange')?.innerText || '';
            const logoUrl = db.settings.logo || '';

            const logoRow = document.createElement('tr');
            logoRow.className = 'print-header-row';
            logoRow.style.display = 'none'; // مخفي في الشاشة، يظهر عند الطباعة عبر CSS
            logoRow.innerHTML = `
                <td colspan="${colCount}" style="text-align:center; border:none; padding:0;">
                    <div class="print-logo-container" style="width:100%;">
                        ${logoUrl ? `<img src="${logoUrl}" class="print-logo-img" style="width:100%; max-height:150px; object-fit:contain; display:block;" alt="شعار الشركة">` : ''}
                    </div>
                    <div class="print-company-name" style="font-size:24px; font-weight:bold; margin:10px 0 4px;">${companyName}</div>
                    <div class="print-report-title" style="font-size:18px; font-weight:bold; margin:4px 0;">كشف تعقيب الموظفين</div>
                    <div class="print-date-range" style="font-size:14px; color:#666; margin-bottom:10px;">${dateRange}</div>
                </td>
            `;
            thead.prepend(logoRow);
        }
    }

    let filteredEmp = db.employees.filter(e => {
>>>>>>> REPLACE
```
////////////////////////
تم إصلاح المشكلة وحلّها بشكل جذري. إليك ملخص التعديل النهائي:

## المشكلة الأصلية
عند الضغط على Ctrl+P مباشرة، كان رأس الصفحة (الشعار + اسم الشركة + العنوان) يختفي، لأن `printHeader` كان عنصراً منفصلاً خارج الجدول وتم إخفاؤه في CSS.

## الحل - ثلاث تعديلات بسيطة

### 1. `tageep.js` - دالة `renderMainTable()`
**تمت إضافة كود** يضيف صف شعار (`print-header-row`) **مباشرة داخل `thead` الأصلي** للجدول في لوحة attendance. هذا الصف مخفي (`display:none`) في وضع العرض العادي ولا يظهر على الشاشة.

### 2. `tageep.css` - تحسينات الطباعة
**تمت إضافة قواعد CSS** داخل `@media print` تظهر صف الشعار عند الطباعة فقط (`display: table-row !important`) وتجعل الصورة تمتد بعرض 100% من الصفحة.

### 3. `tageep.js` - دالة `printVisibleTable()`
**تم إزالة** التعديل السابق غير الضروري الذي كان يضيف الشعار مرة أخرى عند الطباعة عبر زر الطباعة.

## النتيجة النهائية
- ✅ الشعار يمتد بعرض رأس الصفحة بالكامل (width: 100%)
- ✅ الشعار يظهر عند Ctrl+P
- ✅ الشعار يتكرر في كل صفحة طباعة (لأنه داخل `thead` والمتصفح يعيد `table-header-group` تلقائياً)
- ✅ لا يؤثر على مظهر التطبيق في وضع العرض العادي
- ✅ الكود الأصلي موثّق بتعليقات توضح ما تم تغييره
///////////////////
تم تعديل وظيفة الطباعة (printVisibleTable) في ملف `apps/tageep/tageep/public/js/tageep.js` كالتالي:

1. **تم إدراج رأس الصفحة داخل `thead` الجدول المنسوخ مباشرة قبل الطباعة** - عند استدعاء `printVisibleTable()`، يتم نسخ الجدول المرئي، ثم إضافة صف خاص (`tr.print-header-row`) داخل `thead` يحتوي على:
   - الشعار (إن وُجد)
   - اسم الشركة
   - عنوان التقرير "كشف تعقيب الموظفين"
   - تاريخ الفترة (من - إلى)

   هذا يضمن أن الشعار والعنوان **يتكرران في كل صفحة** عند الطباعة بفضل CSS `display: table-header-group`.

2. **تم تعطيل الكود القديم في `renderMainTable`** - الكود الذي كان سابقاً يضيف صف الشعار إلى جدول التاب الرئيسي فقط (مما كان يمنع ظهوره عند طباعة جداول أخرى مثل التقارير). تم تحويله إلى تعليق (معطل) مع شرح كامل، ويمكن الرجوع إليه عند الحاجة.

3. **التعديل يعمل لجميع الجداول** - سواء جدول التعقيب الرئيسي أو جدول التقارير أو أي جدول آخر، لأن المنطق أصبح في `printVisibleTable` وليس مقيداً بجدول معين.

الملفات المعدلة:
- `apps/tageep/tageep/public/js/tageep.js` - التعديل الرئيسي
- ملف CSS الحالي (`tageep.css`) يدعم بالفعل الطباعة عبر class `print-header-row` (مخفي في الشاشة، يظهر فقط عند الطباعة)
//////////////////////////////
اريد في تطبيق tageep  ان تعمل التعديل التالي 
اريد تضيف لي حقل في واجهة الاعدادات 
بحيث في هذا الحقل اختار حجم الورق مثلا A4 or A5  وغيرها من الاحجام 
بحيث عند اختيار حجم محدد يتم عرض  واجهة معاينة الطباعة بالحجم المختار ويقسم واجهة المعاينة بالحجم الورق المختار 
واريد يظهر راس الطباعة واريد راس الجدول من اسماء الاعمدة يظهر في كل صفحة يتم طباعتها
اريد في واجهة المعاينة يظهر عدد الصفحات الطباعه حسب نوع الورق المختار
//////////////////////////////
عند الطباعة بشكل العمودي يتم طباعة الشعار والعنوان في كل صفحة 

لاكن هناك مشكلة لم يطبع الجدول  كامل  ينقص ثلاثة اعمدة من الجدول لم يتم طباعتها 

وعند الطباعة بشكل افقي 

يتم طباعة الجدول كامل لاكن لم يطبع الشعار والعنوان في كل صفحة 

اريد تحل هذه المشكلتين 
///////////////////////
تم تعديل الشعار ليكون بعرض الجدول الكامل (وليس في الوسط):
- تم تغيير نمط الصورة من `margin:0 auto;max-height:60px;object-fit:contain` إلى `margin:0;padding:0;height:auto;object-fit:fill`
- هذا يجعل الشعار يمتد بعرض الجدول الكامل من اليمين إلى اليسار
- تم تعديل كود الطباعة في دالتي `updatePrintPreview` (المعاينة) و `printVisibleTable` (الطباعة المباشرة)
- تم تعديل CSS لإزالة `max-height` من صورة الشعار وجعلها بعرض كامل

**ملخص جميع التعديلات التي تمت في الجلسة:**
1. ✅ إصلاح الطباعة العمودية - الأعمدة الناقصة (حجم خط 7px، هامش 8mm)
2. ✅ إصلاح الطباعة الأفقية - تكرار الشعار (قراءة orientation من localStorage)
3. ✅ إضافة القوائم القابلة للبحث في حقول الموظف والفرع
4. ✅ التحديث التلقائي بعد الإضافة (saveDB → refreshCurrentView)
5. ✅ استعادة شكل التعقيب الرئيسي الأصلي (تنسيق التاريخ العربي، 0ح/1غ/1س/0م، صف المجموع)
6. ✅ تحسين وضوح الخط في الطباعة (print-color-adjust: exact، 10pt)
7. ✅ الشعار بعرض الجدول الكامل (object-fit:fill, margin:0)

////////////////////////////
تم إصلاح جميع المشاكل في ملفي `tageep.js` و `tageep.css`:

## ✅ 1. القوائم المنسدلة القابلة للبحث (Searchable Select)
- تم إصلاح مشكلة عدم ظهور الاسم المختار في الحقل - تم استخدام `mousedown` + `click` مع `preventDefault()` لمنع تعارض فقدان التركيز
- تمت إضافة أنماط CSS جديدة للحقل والقائمة
- القوائم التي تعمل الآن بالبحث: الفرع، الموظف، الدوام، الفترة

## ✅ 2. استعادة شكل التعقيب الرئيسي الأصلي
- تمت إعادة تنسيق أعمدة التاريخ إلى الصيغة العربية الأصلية: `toLocaleDateString('ar-EG', { day: 'numeric', month: 'long' })`
- تمت إعادة محتوى خلايا التاريخ إلى الشكل الأصلي: `0ح` (حاضر)، `1غ` (غائب)، `1س` (سنوية)، `0م` (مناسبة)
- تمت إضافة صف **المجموع** أسفل الجدول مع إجمالي الأيام والأجور
- تمت إعادة تلوين الخلايا: أحمر للغياب، أخضر للحضور، برتقالي للإجازات
- تمت إعادة زر "تسجيل الحالة" بدلاً من "تسجيل"

## ✅ 3. تحسين وضوح الخط في الطباعة
- تمت إضافة CSS: `-webkit-print-color-adjust: exact` للحفاظ على الألوان في الطباعة
- تمت إضافة CSS: `print-color-adjust: exact` لدعم المتصفحات الحديثة
- تم ضبط حجم الخط في الطباعة على `10pt`

## ✅ 4. التحديث التلقائي بعد الإضافة
- دالة `saveDB()` الآن تستدعي `refreshCurrentView()` لتحديث الجدول فوراً
- دالة `refreshAllDropdowns()` تحدث جميع القوائم المنسدلة تلقائياً
//////////////////////
تم تعديل ملف `apps/tageep/tageep/public/js/tageep.js` لحل مشكلتي الطباعة:

### ✅ المشكلة 1: الطباعة العمودية - الأعمدة الناقصة
- تم تغيير حجم الخط ديناميكياً حسب الاتجاه: `7px` للخلايا و `9px` للترويسة في الوضع العمودي (بدلاً من 12px)
- تم تقليل حشوة الخلايا (padding) من `6px 8px` إلى `1.5px 2px` في الوضع العمودي
- تم تصغير الهامش من `15mm` إلى `10mm` لإعطاء مساحة أكبر للجدول
- تم إضافة `word-break: keep-all` لمنع تكسير النص بشكل عشوائي

### ✅ المشكلة 2: الطباعة الأفقية - الشعار والعنوان لا يتكرران في كل صفحة
- دالة `printVisibleTable` (الطباعة المباشرة) كانت تستخدم `@page { size: landscape }` بشكل ثابت وتتجاهل إعدادات المستخدم. تم تعديلها لقراءة الاتجاه وحجم الورق من `localStorage`:
  ```js
  const orientation = localStorage.getItem('tageep_orientation') || 'landscape';
  const paperSize = localStorage.getItem('tageep_paper_size') || 'A4';
  ```
- تم إضافة قاعدة CSS `thead img { max-height: 50px !important; }` لتحديد أقصى ارتفاع للشعار في الرأس لضمان تكراره
- تم تخفيض `max-height` للشعار في HTML من `150px` إلى `80px`
- دالتا `printVisibleTable` و `updatePrintPreview` تستخدمان الآن نفس المنطق تماماً للتعامل مع أحجام الخطوط والهوامش حسب اتجاه الطباعة